<{{#ifAny menu-toggle--IsDiv menu-toggle--IsSplitButton menu-toggle--IsAction}}div{{else}}button{{/ifAny}} class="{{pfv}}menu-toggle
  {{setModifiers
    menu-toggle--IsPrimary='pf-m-primary'
    menu-toggle--IsSecondary='pf-m-secondary'
    menu-toggle--IsPlain='pf-m-plain'
    menu-toggle--IsText='pf-m-text'
    menu-toggle--IsExpanded='pf-m-expanded'
    menu-toggle--IsTypeahead='pf-m-typeahead'
    menu-toggle--IsAction='pf-m-action'
    menu-toggle--IsSplitButton='pf-m-split-button'
    menu-toggle--IsDisabled='pf-m-disabled'
    menu-toggle--modifier=menu-toggle--modifier
  }}"

  {{#unless (concat menu-toggle--IsDiv menu-toggle--IsSplitButton menu-toggle--IsAction)}}
    type="button"
    {{#if menu-toggle--IsExpanded}}
      aria-expanded="true"
    {{else}}
      aria-expanded="false"
    {{/if}}
    {{#if menu-toggle--IsDisabled}}
      disabled
    {{/if}}
  {{/unless}}

  {{#if menu-toggle--id}}
    id="{{menu-toggle--id}}"
  {{/if}}

  {{#if menu-toggle--attribute}}
    {{{menu-toggle--attribute}}}
  {{/if}}>

  {{#if @partial-block}}
    {{> @partial-block}}
  {{else}}

    {{!-- if menu toggle has checkbox --}}
    {{#if menu-toggle--IsCheck}}
      {{> check check--id=(concat menu-toggle--id '-check')}}
    {{/if}}

    {{#ifAny menu-toggle--icon menu-toggle--HasAvatar menu-toggle--text menu-toggle--badge--count}}
      {{#> menu-toggle-main}}

        {{#if menu-toggle--icon}}
          {{> menu-toggle-icon}}
        {{/if}}

        {{#if menu-toggle--HasAvatar}}
          {{#> menu-toggle-icon}}
            {{> avatar avatar--modifier='pf-m-sm'}}
          {{/menu-toggle-icon}}
        {{/if}}

        {{#if menu-toggle--text}}
          {{> menu-toggle-text}}
        {{/if}}

        {{#if menu-toggle--badge--count}}
          {{#> menu-toggle-count}}
            {{> badge badge--modifier="pf-m-unread" badge--text=menu-toggle--badge--count}}
          {{/menu-toggle-count}}
        {{/if}}

      {{/menu-toggle-main}}
    {{/ifAny}}

    {{#> menu-toggle-controls}}
      {{#> menu-toggle-button button--type=(ternary (hasValue (array menu-toggle--IsDiv menu-toggle--IsAction menu-toggle--IsSplitButton)) 'button' 'span')}}
        {{#> button button--IsPlain=true}}
          {{> menu-toggle-toggle-icon}}
        {{/button}}
      {{/menu-toggle-button}}
    {{/menu-toggle-controls}}

  {{/if}}
</{{#ifAny menu-toggle--IsDiv menu-toggle--IsSplitButton menu-toggle--IsAction}}div{{else}}button{{/ifAny}}>
