{{!-- TODO: automate `td/th` based up `IsThead/IsTbody` and make overridable --}}
{{!-- TODO: automate attribution --}}
{{!-- table-test--tag=(ternary IsThead 'th' 'td') --}}

{{init
  root='table'
}}
{{classConfig
  (entry 'action-class' 'table-cell--action-class')
  (entry 'toggle-class' 'table-cell--toggle-class')
  (entry 'empty-class' 'table-cell--empty-class')
}}

{{#>
  config
  table-test--tag=(ternary IsThead 'th' 'td')
  classlist=(concat
    (extendRoot table-test--tag)
    (classConfig
      (entry 'action-class' 'table-cell--action-class')
      (entry 'toggle-class' 'table-cell--toggle-class')
      (entry 'empty-class' 'table-cell--empty-class')
    )
  )
}}

  {{!-- table-cell--root=(extendRoot table-test--tag) --}}
  {{!-- table-cell--modifiers=(ifTruthy
    (entry table-cell--IsAction table-cell--action-class)
    (entry table-cell--IsCheck (concat table-cell--root '__check'))
    (entry table-cell--IsEmpty (concat table-cell--root '__empty'))
    (entry table-cell--IsOverflowMenu (concat table-cell--root 'table__action'))
    (entry table-cell--IsToggle (concat table-cell--root '__toggle'))
  ) --}}

{{!-- {{


  table-cell--modifiers=(ifTruthy
    (entry table-cell--IsAction table-cell--action-class)
    (entry table-cell--IsCheck (concat table-cell--root '__check'))
    (entry table-cell--IsEmpty (concat table-cell--root '__empty'))
    (entry table-cell--IsOverflowMenu (concat table-cell--root 'table__action'))
    (entry table-cell--IsToggle (concat table-cell--root '__toggle'))
  )

}} --}}


{{!-- (ifTruthy
  (entry table-cell--IsAction table-cell--action-class)
  (entry table-cell--IsCheck (concat table-cell--root '__check'))
  (entry table-cell--IsEmpty (concat table-cell--root '__empty'))
  (entry table-cell--IsOverflowMenu (concat table-cell--root 'table__action'))
  (entry table-cell--IsToggle (concat table-cell--root '__toggle'))
) --}}

  {{!-- table-cell--root=(extendRoot table-test--tag) --}}
  {{!-- (ifTruthy
    (entry table-cell--IsAction table-cell--action-class)
    (entry table-cell--IsCheck (concat table-cell--root 'check'))
    (entry table-cell--IsEmpty (concat table-cell--root 'empty'))
    (entry table-cell--IsOverflowMenu (concat table-cell--root 'table__action'))
    (entry table-cell--IsToggle (concat table-cell--root '__toggle'))
  ) --}}

{{!-- {{debug}} --}}

<{{table-test--tag}} class="{{this.classlist}}"
  role='cell'
  {{#if table-cell--attribute}}
    {{{table-cell--attribute}}}
  {{/if}}>

  {{#if @partial-block}}
    {{> @partial-block}}
  {{/if}}
</{{table-test--tag}}>

{{/config}}
